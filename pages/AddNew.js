import { Container } from "react-bootstrap";

const AddNew = () => {
    return (
        <Container
            className="d-flex align-items-center justify-content-center"
            style={{ height: "85vh" }}
        >
            <h1>with this page user can add new table</h1>
        </Container>
    );
};

export default AddNew;
